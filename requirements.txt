# Core pipeline dependencies for oil_spill_pipeline.py
# scikit-learn is pinned to the version oil_spill_detector.pkl was actually
# trained with (confirmed via unpickling warning: "Trying to unpickle
# estimator StandardScaler from version 1.6.1"). Do not bump this without
# retraining/re-pickling the model.
scikit-learn==1.6.1

numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
joblib==1.5.3
tifffile==2026.3.3
rasterio==1.4.4
Pillow==12.2.0

# Not installed in the environment this was pinned from; versions below are
# pip's current dry-run resolution against the pins above (confirmed
# conflict-free), not read from an installed package.
pyproj==3.7.2
scikit-image==0.26.0
